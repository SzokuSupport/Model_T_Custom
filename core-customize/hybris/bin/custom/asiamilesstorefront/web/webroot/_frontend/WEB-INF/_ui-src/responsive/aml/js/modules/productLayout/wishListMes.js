

    export default function wishListShowMessage() {
        /* let $wishList = $('.J-wishList'),

        $wishListIconInactive = $('.J-wishList > .icon-wish-inactive'),
        $wishListIconActive = $('.J-wishList > .icon-wish-active'),

        $wishListMes = $('.J-wishList-message'),

        $wishActive = $('.J-wishList-message > .icon-wish-active'),
        $wishCancel = $('.J-wishList-message > .icon-wish-list'),
        $wishActiveText = $('.J-add'),
        $wishInactiveText = $('.J-already-add'),
        isShow = true

        $wishList.on('click', function() {
            if(isShow) {
                $wishListMes.addClass('wishFlexActive');

                $wishListIconInactive.show();
                $wishListIconActive.show();
                $wishListIconActive.addClass('wishBigImage');

                $wishActive.show();
                $wishCancel.hide();

                $wishInactiveText.hide();
                $wishActiveText.show();
                isShow = false;
            } else {
                $wishListMes.addClass('wishFlexActive');

                $wishListIconInactive.show();
                $wishListIconActive.show();
                $wishListIconActive.addClass('wishBigImage');

                $wishActive.hide();
                $wishCancel.show();

                $wishInactiveText.show();
                $wishActiveText.hide();
                isShow = true;
            }
            
            setTimeout(function(){
                $wishListIconInactive.show();
                $wishListIconActive.hide();
                $wishListIconActive.removeClass('wishBigImage');
                $wishListMes.removeClass('wishFlexActive');

            },3000);

            // $.ajax({
            //     url: url,
            //     type: 'post',
            //     dataType: 'json',
            //     async: true,
            //     success: function (data) {

            //     },
            //     error: function(data) {
            //       $errorMsg.text(data.errorMsg)
            //     }
            // })
        });
        */

    }
    